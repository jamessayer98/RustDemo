extern crate somerust;

// use somerust::guess;
use somerust::types;

fn main() {
    // guess::guessit();
    types::some_slice();
    types::some_struct();
    types::some_enum();
    types::some_list();
}
