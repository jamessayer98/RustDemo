extern crate somerust;

// use somerust::guess;
// use somerust::types;
use somerust::flow;
use somerust::matchs;

fn main() {
    // guess::guessit();
    // types::some_slice();
    // types::some_struct();
    // types::some_enum();
    // types::some_list();
    // types::some_const();
    flow::ifelse();
    flow::some_loop();
    matchs::some_matchs();
}
